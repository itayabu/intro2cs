
=============================
=  README for Ex10  =
=============================

Number class:
holds a double number in a Double class.

Variable class:
holds a variable name, can evaluate the name to a number using a map

Operator:
calculate and represent simple math actions (plus, minus mult and div)

Evaluator class:
holds a map that can contain variable names and their values. recieve an expression and calculate it recursivly

Parser class:
recusively parse an expression to an operation tree, where leaves are number or variables
